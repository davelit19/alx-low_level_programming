#include <stdio.h>

/**
 * main - prints out the string of the printf function
 *
 * Return: 0 if success
 */
int main(void)
{
iprintf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}

